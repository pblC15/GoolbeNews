import Link from 'next/link'
export default function PostCard({post}:{post:any}){return <article className="group overflow-hidden rounded-2xl border border-slate-200 bg-white transition hover:-translate-y-0.5 hover:shadow-lg">{post.cover_url&&<Link href={`/post/${post.slug}`} className="block overflow-hidden"><img src={post.cover_url} alt={post.title} className="aspect-[16/9] w-full object-cover transition duration-500 group-hover:scale-[1.03]"/></Link>}<div className="p-5">{post.category_name&&<span className="text-xs font-bold uppercase tracking-wider text-sky-700">{post.category_name}</span>}<h3 className="mt-2 text-xl font-black leading-tight tracking-tight text-slate-950"><Link href={`/post/${post.slug}`} className="hover:text-sky-700">{post.title}</Link></h3>{post.excerpt&&<p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{post.excerpt}</p>}</div></article>}
