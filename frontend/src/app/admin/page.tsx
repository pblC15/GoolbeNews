'use client'
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {adminApi} from '@/lib/adminApi'
export default function Dashboard(){const [posts,setPosts]=useState<any[]>([]),[cats,setCats]=useState<any[]>([]),[users,setUsers]=useState<any[]>([]); useEffect(()=>{Promise.all([adminApi('/posts/admin/list'),adminApi('/categories'),adminApi('/users').catch(()=>[])]).then(([p,c,u])=>{setPosts(p);setCats(c);setUsers(u)})},[]); const published=posts.filter(p=>p.published).length
return <div className="space-y-7"><div><h1 className="text-3xl font-black tracking-tight text-slate-950">Visão geral</h1><p className="mt-1 text-slate-500">Tudo o que precisa para gerir a redação num só lugar.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Notícias',posts.length],['Publicadas',published],['Rascunhos',posts.length-published],['Categorias',cats.length],['Utilizadores',users.length]].slice(0,4).map(([l,v])=><div key={String(l)} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{l}</p><p className="mt-2 text-3xl font-black text-slate-900">{v}</p></div>)}</div><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-bold">Últimas notícias</h2><Link href="/admin/posts" className="text-sm font-semibold text-sky-700">Ver todas</Link></div><div className="divide-y">{posts.slice(0,6).map(p=><div key={p.id} className="flex items-center justify-between gap-4 py-3"><div className="min-w-0"><p className="truncate font-medium">{p.title}</p><p className="text-xs text-slate-500">{p.category_name||'Sem categoria'} · {p.author_name||'Autor'}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${p.published?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{p.published?'Publicado':'Rascunho'}</span></div>)}</div></section></div>}
